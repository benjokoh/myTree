function getRandomNumber(upper) {
   return Math.floor( Math.random() * upper ) + 1;
  }
  
  for(i = 0; i< 10; i ++){
    console.log( `The random number is ${getRandomNumber(35)}` );
  }


for( let counter = 5; counter <= 100; counter++ ){
     console.log(counter);// code to run
  }